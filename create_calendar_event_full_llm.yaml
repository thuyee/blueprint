blueprint:
  name: Create Calendar Event
  author: luuquangvu
  source_url: https://github.com/luuquangvu/tutorials/blob/main/create_calendar_event_full_llm.yaml
  description: |-
    # Tool help to create calendar events used for Voice Assistant

    ## Blueprint Setup

    ### Required

    - A calendar entity with write permissions to save events.

    ### Optional

    - Adjust the prompts for each field used in the script. The descriptions guide the LLM to provide the correct input.

    ### Note

    - Google Calendar requires Read/Write access.
    - Provide a concise and precise description for the script. This will be utilized by the LLM to understand it should use this script for creating calendar events.
    - Make sure to expose the script to Assist after the script has been saved.
    - Do not alter the default script name.
    - Once the script is created, click the three dots in the top right corner, choose "Edit in YAML," and remove the `description: ...` line to restore the default. This step is important because it helps the LLM better understand the script's purpose.
  domain: script
  homeassistant:
    min_version: 2024.12.0
  input:
    calendar_settings:
      name: Settings for Calendar
      icon: mdi:calendar
      description: You can use these settings to configure a Calendar.
      input:
        calendar:
          name: Calendar
          selector:
            entity:
              filter:
                - domain: calendar
    custom_settings:
      name: Custom Settings
      icon: mdi:tools
      description: You can use these settings to configure the maximum number of simultaneous processes to run at a time.
      collapsed: true
      input:
        simultaneous:
          name: Simultaneous Processes
          selector:
            number:
              min: 10
              max: 50
          default: 30
    prompt_settings:
      name: Prompt settings for the LLM
      icon: mdi:robot
      description: You can use these settings to finetune the prompts for your specific LLM (model). In most cases the defaults should be fine.
      collapsed: true
      input:
        summary_prompt:
          name: Summary Prompt
          description: A brief title or summary of the calendar event, provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            Event subject/title
        description_prompt:
          name: Description Prompt
          description: Detailed information about the calendar event, provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            Event details
        start_date_time_prompt:
          name: Start Time Prompt
          description: The starting date and time for timed events (YYYY-MM-DD HH:MM:SS), provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            Timed events (YYYY-MM-DD HH:MM:SS)
        end_date_time_prompt:
          name: End Time Prompt
          description: The ending date and time for timed events (YYYY-MM-DD HH:MM:SS), provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            End date/time (YYYY-MM-DD HH:MM:SS). Optional, default start + 1h
        start_date_prompt:
          name: Start Date Prompt
          description: The starting date for all-day events (YYYY-MM-DD), provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            All-day events (YYYY-MM-DD)
        end_date_prompt:
          name: End Date Prompt
          description: The ending date for all-day events (YYYY-MM-DD), provided by the AI
          selector:
            text:
              multiline: true
          default: |-
            End date (YYYY-MM-DD). Optional, default start date
mode: queued
max: !input simultaneous
max_exceeded: silent
description: Adds a new event to a specified calendar
variables:
  version: 20260821
fields:
  summary:
    selector:
      text:
    name: Summary
    required: true
    description: !input summary_prompt
  description:
    selector:
      text:
        multiline: true
    name: Description
    description: !input description_prompt
  start_date_time:
    selector:
      datetime:
    name: Start Date Time
    description: !input start_date_time_prompt
  end_date_time:
    selector:
      datetime:
    name: End Date Time
    description: !input end_date_time_prompt
  start_date:
    selector:
      date:
    name: Start Date
    description: !input start_date_prompt
  end_date:
    selector:
      date:
    name: End Date
    description: !input end_date_prompt
sequence:
  - variables:
      summary: "{{ summary | default('') | trim }}"
      description: "{{ description | default('') | trim }}"
      start_date_time: "{{ start_date_time | default('') | trim }}"
      start_date: "{{ start_date | default('') | trim }}"
      end_date_time: >-
        {% if end_date_time | default('') | trim != '' %}
          {{ end_date_time | trim }}
        {% elif start_date_time | default('') | trim != '' %}
          {% set sdt = start_date_time | as_datetime(default=none) %}
          {{ ((sdt + timedelta(hours=1)) | as_local).isoformat() if sdt is not none else '' }}
        {% else %}
          ''
        {% endif %}
      end_date: >-
        {% set sd = start_date | as_datetime(default=none) %}
        {% set ed_raw = end_date | default('', true) | trim %}
        {% if ed_raw != '' %}
          {% set ed = ed_raw | as_datetime(default=none) %}
          {% if sd is not none and ed is not none and sd.date() == ed.date() %}
            {{ ((ed + timedelta(days=1)) | as_local).date() | string }}
          {% else %}
            {{ ed_raw }}
          {% endif %}
        {% elif sd is not none %}
          {{ ((sd + timedelta(days=1)) | as_local).date() | string }}
        {% else %}
          ''
        {% endif %}
      calendar: !input calendar
  - alias: Check if variables were set correctly
    if:
      - condition: template
        value_template: >-
          {{ (not summary) or (not start_date_time and not start_date) }}
    then:
      - alias: Set variable for error message
        variables:
          response:
            error: The event could not be added due to invalid or misconfigured input parameters.
      - alias: Stop the script
        stop: The event could not be added due to invalid or misconfigured input parameters.
        response_variable: response
  - alias: Verify whether the calendar entity has write permissions.
    if:
      - condition: template
        value_template: >-
          {% set feats = (state_attr(calendar, 'supported_features') | int(0)) -%}
          {{ not (feats | bitwise_and(1)) }}
    then:
      - alias: Set variable for error message
        variables:
          response:
            error: The event could not be added because the calendar entity lacks write permissions.
      - alias: Stop the script
        stop: The event could not be added because the calendar entity lacks write permissions.
        response_variable: response
  - alias: Validate ambiguous start inputs
    if:
      - condition: template
        value_template: >-
          {{ (start_date_time | default('') | trim != '') and (start_date | default('') | trim != '') }}
    then:
      - alias: Set variable for error message
        variables:
          response:
            error: >-
              Please provide only one start parameter: either start_date_time (timed) or start_date (all-day), not both.
      - alias: Stop the script
        stop: >-
          Please provide only one start parameter: either start_date_time (timed) or start_date (all-day), not both.
        response_variable: response
  - alias: Validate end inputs for timed start
    if:
      - condition: template
        value_template: >-
          {{ (start_date_time | default('') | trim != '') and (end_date_time | default('') | trim == '') }}
    then:
      - variables:
          response:
            error: For timed events, end_date_time is required and must be included.
      - stop: For timed events, end_date_time is required and must be included.
        response_variable: response
  - alias: Validate end inputs for all-day start
    if:
      - condition: template
        value_template: >-
          {{ (start_date | default('') | trim != '') and (end_date | default('') | trim == '') }}
    then:
      - variables:
          response:
            error: For all-day events, end_date is required and must be included.
      - stop: For all-day events, end_date is required and must be included.
        response_variable: response
  - alias: Disallow type mismatch for timed start
    if:
      - condition: template
        value_template: >-
          {{ (start_date_time | default('') | trim != '') and (end_date | default('') | trim != '') }}
    then:
      - variables:
          response:
            error: >-
              Do not mix types: timed start requires end_date_time, not end_date.
      - stop: >-
          Do not mix types: timed start requires end_date_time, not end_date.
        response_variable: response
  - alias: Disallow type mismatch for all-day start
    if:
      - condition: template
        value_template: >-
          {{ (start_date | default('') | trim != '') and (end_date_time | default('') | trim != '') }}
    then:
      - variables:
          response:
            error: >-
              Do not mix types: all-day start requires end_date, not end_date_time.
      - stop: >-
          Do not mix types: all-day start requires end_date, not end_date_time.
        response_variable: response
  - alias: Validate end after start for timed
    if:
      - condition: template
        value_template: >-
          {{ (start_date_time | as_datetime) is datetime and (end_date_time | as_datetime) is datetime and (end_date_time | as_datetime) < (start_date_time | as_datetime) }}
    then:
      - variables:
          response:
            error: end_date_time must be on or after start_date_time.
      - stop: end_date_time must be on or after start_date_time.
        response_variable: response
  - alias: Validate end after start for all-day
    if:
      - condition: template
        value_template: >-
          {{ (start_date | as_datetime) is datetime and (end_date | as_datetime) is datetime and ((end_date | as_datetime).date()) < ((start_date | as_datetime).date()) }}
    then:
      - variables:
          response:
            error: end_date must be on or after start_date.
      - stop: end_date must be on or after start_date.
        response_variable: response
  - choose:
      - alias: Timed event (requires end_date_time)
        conditions:
          - condition: template
            value_template: >-
              {{ (start_date_time | as_datetime) is datetime and (end_date_time | as_datetime) is datetime }}
        sequence:
          - action: calendar.create_event
            target:
              entity_id: !input calendar
            data:
              summary: "{{ summary }}"
              description: "{{ description }}"
              start_date_time: "{{ (start_date_time | as_datetime | as_local).isoformat() }}"
              end_date_time: "{{ (end_date_time | as_datetime | as_local).isoformat() }}"
          - alias: Return success message (timed)
            variables:
              response:
                message: >-
                  Event created: {{ summary }} ({{ (start_date_time | as_datetime).strftime('%Y-%m-%d %H:%M') }} to {{ (end_date_time | as_datetime).strftime('%Y-%m-%d %H:%M') }}) in {{ state_attr(calendar, 'friendly_name') or calendar }}.
          - stop: ""
            response_variable: response
      - alias: All-day event (requires end_date)
        conditions:
          - condition: template
            value_template: >-
              {{ (start_date | as_datetime) is datetime and (end_date | as_datetime) is datetime }}
        sequence:
          - action: calendar.create_event
            target:
              entity_id: !input calendar
            data:
              summary: "{{ summary }}"
              description: "{{ description }}"
              start_date: "{{ (start_date | as_datetime | as_local).date() }}"
              end_date: "{{ (end_date | as_datetime | as_local).date() }}"
          - alias: Return success message (all-day)
            variables:
              response:
                message: >-
                  {% set s_date = (start_date | as_datetime).date() %}
                  {% set e_date = (end_date | as_datetime).date() %}
                  {% set diff_days = (e_date - s_date).days %}
                  {% if diff_days <= 1 %}
                    Event created: {{ summary }} (all day on {{ s_date }}) in {{ state_attr(calendar, 'friendly_name') or calendar }}.
                  {% else %}
                    Event created: {{ summary }} ({{ s_date }} to {{ (e_date - timedelta(days=1)) }}) in {{ state_attr(calendar, 'friendly_name') or calendar }}.
                  {% endif %}
          - stop: ""
            response_variable: response
    default:
      - alias: Invalid date configuration
        variables:
          response:
            error: >-
              Invalid date configuration. Specify either start_date_time + end_date_time (timed) or start_date + end_date (all-day), and ensure end is on/after start.
      - stop: Invalid date configuration
        response_variable: response
